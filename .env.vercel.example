# Vercel Environment Variables
# Copy these to your Vercel project settings under Environment Variables

# Redis Configuration (REQUIRED)
# You need a Redis instance accessible from the internet
# Options:
# 1. Upstash Redis (recommended for Vercel): https://upstash.com/
# 2. Redis Labs: https://redis.com/try-free/
# 3. AWS ElastiCache, Azure Cache for Redis, etc.

REDIS_URL=redis://your-redis-host:6379

# Queue Configuration (Optional - uses defaults if not set)
DEFAULT_QUEUE=default
TASK_TIMEOUT=300
MAX_RETRIES=3

# Logging (Optional)
LOG_LEVEL=INFO

# IMPORTANT NOTES:
# 1. DO NOT commit your actual Redis URL to version control
# 2. Make sure your Redis instance is accessible from Vercel's deployment regions
# 3. For production, use Redis with authentication: redis://username:password@host:port
# 4. Upstash Redis provides a free tier perfect for testing: https://upstash.com/
